---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springingboot                  # Ingress name
  labels:
    app: springingboot
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springingboot.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springingboot               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
